join(
  select(
    variables('imagesArray'), //array change to map with code
    concat(
      '<a href="', item()?['link'], '">',
      last(split(item()?['link'], '/')),
      '</a>'
    )
  ),
  '<br/>'
)
